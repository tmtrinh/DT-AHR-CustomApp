import React from "react";
import { Flex, Heading } from "@dynatrace/strato-components-preview";
//import { TENANT, LIVE_URL, APPS_URL } from "./Environment";

export const Questionnaire = () => {
    return(
        <Flex flexDirection="column">
          <Heading level={4}>Tenant: </Heading>
        </Flex>
    );
};

//All data is in last 30 days